$(document).ready(function() {

/*	//	Add headings to sections and build nav
		$('section').each(function(cb){

			if($(this).attr('data-title')==undefined)return;
			
			// get title
			var title = $(this).attr('data-title');

			// make id
			var id = title.replace(/\s+/g, '-');

			// add id to sections
//			$(this).attr('id',id);

			// add heading
			var h = document.createElement('h2');
			$(h).html(title)
				.css({'fontSize':'130%','text-transform':'uppercase'})
				.prependTo(this);

			//add nav item
//			var li = document.createElement('li');
//			$(li).html('<a href="#'+id+'">'+title+'</a>').appendTo('#side-nav');
		});
*/		
//		$('#content').scrollspy();
});
